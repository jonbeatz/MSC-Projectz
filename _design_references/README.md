# Design references (non-build)

This folder holds **screenshots, Ideaz captures, and an archived MSC-Projectz-v0 snapshot** for visual reference only.

- **Not imported** by Next.js, Payload, or Tailwind. Safe to ignore for `npm run build`.
- **Repo decision:** keep in Git for now so mood boards and comparisons stay with the project. If clone size becomes painful, move heavy assets to external storage or a separate branch and trim here—do not delete without confirming nothing links to these paths from production code (nothing should).
