---
name: deploy-ftp-node
description: Compatibility wrapper; use Deploy-Profile-Package canonical skill.
---

# Deploy FTP + Node (Compatibility Wrapper)

This skill is deprecated in favor of:

- `Workflow-Portable/Deploy-Profile-Package/SKILL.md`

Use the canonical skill above for current repo deploy flow (`deploy:preflight` -> `pushitlive` -> `final_deploy.zip`).
