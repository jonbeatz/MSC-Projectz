---
name: docs-governance
description: Compatibility wrapper; governance now routed through canonical docs architecture.
---

# Docs Governance (Compatibility Wrapper)

This skill is deprecated as a standalone policy set.

Use:

- `.cursor/docs/Docs-Architecture.md` for canonical docs order
- `Workflow-Portable/Workflow-Ops/SKILL.md` for flow routing
- `Workflow-Portable/Session-Handoff-Restore/SKILL.md` for closeout/checkpoint updates

Authority rule: if docs conflict with scripts, trust `package.json` and update docs drift in the same session.

