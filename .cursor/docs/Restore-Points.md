# Restore Points

Use this file for fast recovery after meaningful milestones. Keep entries short and include exact restore commands.

| Checkpoint | Date | Notes |
| --- | --- | --- |
| RP-2026-04-27-fulldev-v5-primary-line | 2026-04-27 | **Primary branch** is now **`MSC-Projectz-FullDev-v5`** (**`MSC-Projectz-FullDev-v4`** at the same **tip**). **Merge base for feature work:** `3ef90c9` (*feat(calendar)…*). **Tip:** always confirm with `git log -1 --oneline` on `v5` after `git pull`. **Includes:** `/calendar` (mobile scroll matrix, `useIsMaxMd`, bottom agenda `Sheet`, chip long-press + pencil, `div[role="button"]` day cells, border-only selection), vault/task support files, and operator doc alignment. Restore: `git fetch origin && git switch MSC-Projectz-FullDev-v5 && git pull && npm install && npm run dev`. Verify: `npm run verify:next`; smoke **`/calendar`**, `/`, `/admin`, `/tasks` on **3000**. Caveat: do not run `clean:next` / `verify:next` while `next dev` uses the same **`.next`**. |
| RP-2026-04-27-calendar-mobile-command-center | 2026-04-27 | **`/calendar` (mobile + a11y):** Same as **v5** row; historical row name retained. Use **`MSC-Projectz-FullDev-v5`** for current restore. |
| RP-2026-04-27-fulldev-v4-favicon-line | 2026-04-27 | **Primary branch** is now **`MSC-Projectz-FullDev-v4`** (created from `FullDev-v3` at the same commit as this row; advance SHA with `git log -1` on `v4`). **Favicon:** `next.config.mjs` rewrites `GET /favicon.ico` → `/media/msc-icon.png` (served from `./media` via `app/media/[...path]`). Restore: `git fetch origin && git switch MSC-Projectz-FullDev-v4 && git pull && npm install && npm run dev`. Verify: `npm run verify:next`; smoke `/` + `/admin` on **3000**; optional `curl -I http://127.0.0.1:3000/favicon.ico` (expect **200**). |
| RP-2026-04-27-fulldev-v3-main-payload-layout | 2026-04-27 | **App router:** Command Center + auth + login live under `app/(main)/` with `app/(main)/layout.tsx` owning `<html>`/`<body>`; Payload admin/API stay in `app/(payload)/` with Payload `RootLayout`; root `app/layout.tsx` returns `children` only (no nested documents). **Hydration:** `admin.suppressHydrationWarning` + `htmlProps` on `RootLayout`. **Ops:** `scripts/kill-dev-port.mjs`. Branch/commit: `MSC-Projectz-FullDev-v3@d5422dd` (advance SHA after subsequent commits). Restore: `git fetch origin && git switch MSC-Projectz-FullDev-v3 && git reset --hard d5422dd && npm install && npm run dev`. Verify: `npm run verify:next`; smoke `/` + `/admin` on **3000**. |
| RP-2026-04-26-docs-flightpro-deploy | 2026-04-26 | Docs and deploy alignment: **`FlightPro.md`** is the master SOP ( **`pushitlive` → `msc_package_deploy.mjs` → `final_deploy.zip`**, **COPY_PLAN** includes `media/`, `payload.sqlite`, `server.js`, `unzip.php`). **`START-HERE`**, **`Agent-Runbook`** (local recovery without assuming `dev:recover`), **`Flight.md`**, **`ReCall`**, **`Jedi-List`**, **`DeployUpdate`**, **`Spaceship`** updated. Branch: `feature/collaborative-workspace@2f91ba5` (recorded at doc time). Restore: `git fetch origin && git switch feature/collaborative-workspace && git reset --hard 2f91ba5 && npm install && npm run dev`. Verify: `npm run verify:next` before trusting a release; local smoke: `/` and `/admin` on **3000**. Caveat: if your branch moved, replace SHA with the commit you need. |
| RP-2026-04-25-command-center-routes | 2026-04-25 | Route-based Command Center refactor is committed and pushed. Branch/commit: `MSC-Projectz-v2@8602e72fb89b09523e87ad12b7c4179a9f262fec`. Remote: `origin` -> `https://github.com/jonbeatz/MSC-Projectz.git`. Confirmed: `npm run verify:next` passed; local smoke checks returned `200` for `/`, `/dashboard`, `/profile`, `/help`, and `/settings`. Restore: `git fetch origin && git switch MSC-Projectz-v2 && git reset --hard 8602e72fb89b09523e87ad12b7c4179a9f262fec && npm install && npm run dev`. Caveats: one local SQLite backup was intentionally left uncommitted: `payload.sqlite.bak.2026-04-25T18-38-26-162Z`. |
| RP-2026-04-25-code-manager-soft-studio | 2026-04-25 | Code Manager, project-card credential popovers, project-card Explorer bridge, auth diagnostics, and Soft Studio light-mode updates are documented. Latest local commit: `MSC-Projectz-v4@aa764ba` (`Refine project card bridge and auth diagnostics`); branch is ahead of `origin/MSC-Projectz-v4` by one commit. Soft Studio files are currently uncommitted: `app/globals.css`, `components/dashboard-layout.tsx`, and `components/ui/slider.tsx`. Confirmed: `npm run verify:next` passed; local smoke checks returned `200` for `/` and `/admin`. Restore to committed checkpoint: `git switch MSC-Projectz-v4 && git reset --hard aa764ba && npm install && npm run dev`. Caveats: local SQLite backup remains intentionally uncommitted: `payload.sqlite.bak.2026-04-25T18-38-26-162Z`. |
| RP-2026-04-25-tenant-avatar-hardening | 2026-04-25 | Current uncommitted milestone documents tenant isolation and avatar persistence hardening. Runtime vault reads/writes are current-user scoped, Code Manager snippets and project-card credential popovers are user-scoped in localStorage, legacy global project migration is disabled, and profile avatars now upload to tenant-owned Payload media before saving the media ID on `users.avatar`. Confirmed: `npm run verify:next` passed; local smoke checks returned `200` for `/` and `/admin`. Restore to latest committed checkpoint before these edits: `git switch MSC-Projectz-v4 && git reset --hard aa764ba && npm install && npm run dev`. Caveats: changes are not yet committed; local SQLite backup remains intentionally uncommitted: `payload.sqlite.bak.2026-04-25T18-38-26-162Z`. |
