# Archived Scripts

This directory stores one-time migration and forensic scripts that are kept for historical reference.

Rules:

- Do not call these scripts from `package.json` by default.
- Before reusing one, review assumptions against current schema and docs.
- If reactivated for regular use, move it back to `scripts/` and add explicit command wiring.
